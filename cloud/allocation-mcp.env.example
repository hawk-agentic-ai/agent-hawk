# Example environment file for the Allocation MCP server (port 8010)
# Copy to: /etc/hedge/allocation-mcp.env and adjust secrets.

# Supabase
SUPABASE_URL=https://ladviaautlfvpxuadqrb.supabase.co
# Prefer service role for write operations; otherwise fallback to anon
SUPABASE_SERVICE_ROLE_KEY=REPLACE_WITH_SERVICE_ROLE_KEY
# SUPABASE_ANON_KEY=REPLACE_WITH_ANON_IF_NO_SERVICE_ROLE

# Auth token for MCP (optional; set in clients as Bearer)
DIFY_TOOL_TOKEN=REPLACE_WITH_SECURE_RANDOM_TOKEN

# CORS for browser clients (Dify, local dev)
CORS_ORIGINS=http://localhost:3000,http://localhost:4200,https://dify.ai

# Redis (optional)
REDIS_URL=redis://127.0.0.1:6379

# Network
PORT=8010
HOST=0.0.0.0

