# MCP Production Server Dependencies
# HTTP JSON-RPC 2.0 server for Dify Cloud integration

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Database & Cache
supabase==2.0.2
redis[hiredis]==5.0.1

# Data Processing
pydantic==2.5.0
python-multipart==0.0.6

# MCP Protocol (if available, otherwise use custom implementation)
# mcp>=1.0.0  # Uncomment when official MCP package is available

# Utilities
python-dotenv==1.0.0
# asyncio is built-in Python 3.7+ - no need to specify

# HTTP & JSON
httpx>=0.24.0,<0.25.0
aiofiles==23.2.1

# Logging & Monitoring
structlog==23.2.0

# Additional hedge fund processing dependencies
numpy>=1.21.0
pandas>=1.3.0